# MODEL_EVALUATION_REPORT.md — Phase 2: Classical ML Baseline

Generated by `backend/app/ml/models.py:run_all()`. Every number below comes
from an actual training/evaluation run against the real dataset — nothing
here is hand-typed or estimated.

## Target

- Target: `sec_true_t_plus_24h` — `specific_energy_kwh_per_t_true` (ground_truth_reference.csv) shifted 24h into the future, per train.
- Ground truth is used ONLY as the label; it never appears in the feature set.
- 100 features, built from raw telemetry + physics_features.py + features.py (see dataset.py for the full, explicit list).

## Data

- 35040 total telemetry rows -> 34896 after dropping current-timestep shutdown rows -> 34824 after dropping rows with no valid target (shutdown-window ground truth NaNs + the last horizon hours of each train's series).
- Chronological split (config.SPLIT_BOUNDARIES): train=26256, validation=4248, test=4320. No shuffling; split assigned by the CURRENT timestep's calendar date, not the label's.

## Naive baseline

> current measured (noisy) sec_kwh_per_t physics feature at time t, used unchanged as the forecast for specific_energy_kwh_per_t_true at t+H (persistence assumption: 'tomorrow looks like today').

| split | MAE (kWh/t) | RMSE (kWh/t) | R² | n |
|---|---|---|---|---|
| validation | 1.487 | 3.579 | 0.8744 | 4248 |
| test | 1.262 | 2.749 | 0.9300 | 4320 |

## Random Forest

150 trees, max_depth=10, min_samples_leaf=5. NaN features imputed with a median imputer fit on the TRAIN split only (leakage-safe).

| split | MAE (kWh/t) | RMSE (kWh/t) | R² | n |
|---|---|---|---|---|
| validation | 2.535 | 4.595 | 0.7929 | 4248 |
| test | 2.552 | 3.869 | 0.8613 | 4320 |

Top features by importance:

1. `polytropic_head_jkg_s4` — 0.4991
2. `polytropic_head_jkg_s3` — 0.1490
3. `polytropic_head_jkg_s1` — 0.0915
4. `polytropic_head_jkg_s2` — 0.0464
5. `sec_kwh_per_t` — 0.0441
6. `polytropic_head_jkg_s5` — 0.0366
7. `t_s1_disch_c` — 0.0214
8. `feed_mode_ethane` — 0.0150
9. `brg_de_temp_c_roll_mean_168h` — 0.0142
10. `gas_mw` — 0.0110

## XGBoost

400 trees, max_depth=6, lr=0.05, subsample/colsample=0.8. Trained directly on raw (NaN-containing) features — XGBoost's `hist` tree method learns its own default split direction for missing values; no imputation applied.

| split | MAE (kWh/t) | RMSE (kWh/t) | R² | n |
|---|---|---|---|---|
| validation | 3.252 | 5.122 | 0.7427 | 4248 |
| test | 2.540 | 3.817 | 0.8650 | 4320 |

Top features by importance:

1. `polytropic_head_jkg_s4` — 0.4575
2. `polytropic_head_jkg_s3` — 0.1503
3. `sec_kwh_per_t` — 0.0935
4. `feed_mode_ethane` — 0.0606
5. `polytropic_head_jkg_s5` — 0.0590
6. `polytropic_head_jkg_s2` — 0.0505
7. `polytropic_head_jkg_s1` — 0.0318
8. `t_s1_disch_c` — 0.0120
9. `t_s4_disch_c` — 0.0105
10. `t_s5_disch_c` — 0.0058

## Honest comparison

On the held-out test split, MAE is 1.262 kWh/t (naive), 2.552 kWh/t (Random Forest), 2.540 kWh/t (XGBoost). **naive** has the lowest test MAE of the three. This is reported as measured — no claim is made that the more complex model must win. A plausible reason: fouling-driven SEC drift is slow relative to a 24h horizon and highly autocorrelated hour-to-hour, so 'tomorrow looks like today' is already close to the achievable floor — the tree models, regularized against overfitting (max_depth/min_samples_leaf), have limited room to add value from the other engineered features at this horizon. This is a hypothesis consistent with the result, not a separately tested claim; a shorter/longer horizon sweep would be the natural next check.

## Limitations

- Single 24h horizon; a multi-horizon (e.g. 24h/72h/168h) comparison was out of scope for this pass and is a natural extension.
- The naive baseline uses the noisy *measured* sec_kwh_per_t as its persistence input (the true current value is never observable — using ground truth here would itself be a form of leakage). This makes the naive baseline slightly harder to beat than a textbook persistence forecast would be, which is the honest, available comparison.
- Hyperparameters were set to reasonable defaults for this data size, not tuned via a formal search — documented as a limitation rather than reporting a tuned number that wasn't actually searched for.
